from fabric.api import env

#env.use_ssh_config = True
#env.ssh_config_path = 'path_to_ssh_config'

# Define stages servers
STAGES = {
    'test': {
        'hosts': ['user@test.app.com'],
        'path': '/home/path_to_app',
    },
    'production': {
        'hosts': ['user@app.com'],
        'path': '/home/path_to_app',
    },
}
