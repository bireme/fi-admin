- Always use Makefile for compile, build, running tests, installing dependencies, this ensures that all changes are tracked and can be easily reproduced. If a command is not available in the Makefile, create it.
- After implemented changes create a log markdown file in `.ai/logs/` with a summary of changes made, example: `.ai/logs/2024-06-05-updated-llm-integration.
md`